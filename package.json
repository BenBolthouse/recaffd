{
  "name": "recaffd",
  "version": "0.1.0",
  "private": true,
  "main": "backend/bin/www",
  "scripts": {
    "prestart": "npm i --folder backend",
    "start": "npm --prefix backend start",
    "postinstall": "npm i --folder frontend && npm --prefix frontend run build"
  },
  "dependencies": {
    "recaffd-backend": "file:backend",
    "recaffd-frontend": "file:frontend"
  }
}
