import React from 'react';
import PropTypes from 'prop-types';

import './styles.css';

const Register = () => (
  <div>
    Register Component
  </div>
);

Register.propTypes = {};

Register.defaultProps = {};

export default Register;
